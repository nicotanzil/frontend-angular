import { MiniProfileBackground } from './mini-profile-background';

describe('MiniProfileBackground', () => {
  it('should create an instance', () => {
    expect(new MiniProfileBackground()).toBeTruthy();
  });
});
