import { HighestBuyListing } from './highest-buy-listing';

describe('HighestBuyListing', () => {
  it('should create an instance', () => {
    expect(new HighestBuyListing()).toBeTruthy();
  });
});
