import { TopTransactionItem } from './top-transaction-item';

describe('TopTransactionItem', () => {
  it('should create an instance', () => {
    expect(new TopTransactionItem()).toBeTruthy();
  });
});
