import { CommunityGameReviewComment } from './community-game-review-comment';

describe('CommunityGameReviewComment', () => {
  it('should create an instance', () => {
    expect(new CommunityGameReviewComment()).toBeTruthy();
  });
});
