import { GameDiscussionReply } from './game-discussion-reply';

describe('GameDiscussionReply', () => {
  it('should create an instance', () => {
    expect(new GameDiscussionReply()).toBeTruthy();
  });
});
