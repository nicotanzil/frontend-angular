import { CommunityGameReview } from './community-game-review';

describe('CommunityGameReview', () => {
  it('should create an instance', () => {
    expect(new CommunityGameReview()).toBeTruthy();
  });
});
